<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.trimsytrack.permission.READ_KORJOURNAL"
        android:protectionLevel="signature" />

    <permission
        android:name="com.trimsytrack.permission.READ_EVIDENCE"
        android:protectionLevel="signature" />

    <permission
        android:name="com.trimsytrack.permission.READ_VISITED_STORES"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Required for geofence/prompt tracking while the app is not in the foreground (Android 10+). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".TrimsyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_v4"
        android:roundIcon="@mipmap/ic_launcher_round_v4"
        android:label="TrimsyTRACK"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrimsyTRACK">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name=".export.KorjournalProvider"
            android:authorities="${applicationId}.korjournal"
            android:exported="true"
            android:grantUriPermissions="true"
            android:readPermission="com.trimsytrack.permission.READ_KORJOURNAL" />

        <provider
            android:name=".export.EvidenceProvider"
            android:authorities="${applicationId}.evidence"
            android:exported="true"
            android:grantUriPermissions="true"
            android:readPermission="com.trimsytrack.permission.READ_EVIDENCE" />

        <provider
            android:name=".export.VisitedStoresProvider"
            android:authorities="${applicationId}.visitedstores"
            android:exported="true"
            android:grantUriPermissions="false"
            android:readPermission="com.trimsytrack.permission.READ_VISITED_STORES" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TrimsyTRACK.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Firebase passwordless email-link (magic link) sign-in deep links -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="trimsy-d12de.firebaseapp.com"
                    android:pathPrefix="/emailSignIn" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="trimsy-d12de.firebaseapp.com"
                    android:pathPrefix="/__/auth/action" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notifications.PromptActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.RunCompletionActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.TestPingActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".system.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
