<?xml version="1.0" encoding="utf-8"?>
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:insetLeft="24dp"
    android:insetTop="24dp"
    android:insetRight="24dp"
    android:insetBottom="24dp">

    <!--
      Foreground for the adaptive launcher icon.
      Use a bitmap that scales to the available bounds, plus padding to avoid a "zoomed" look.
      (The provided icon is square, so FILL will not distort it.)
    -->
    <bitmap
        android:src="@drawable/icon"
        android:gravity="fill" />

</inset>
